{
  "name": "felix-custom-skills",
  "private": true,
  "description": "Custom skills for Felix Agent — copy into workspace/catalog/skills/ to deploy",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "typescript": "^5.8.3",
    "vitest": "^3.0.7"
  }
}

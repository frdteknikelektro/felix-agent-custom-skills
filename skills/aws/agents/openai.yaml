interface:
  display_name: "AWS"
  short_description: "Inspect and administer AWS resources with separate read and write permissions"
  default_prompt: "Inspect AWS state or perform an explicitly requested AWS admin operation."
policy:
  allow_implicit_invocation: true

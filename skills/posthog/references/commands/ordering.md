# Ordering

- Use `?orderBy=-timestamp` for newest-first descending results.
- Use `?orderBy=timestamp` for chronological ascending results.
- Confirm the endpoint supports ordering before adding the parameter; if unsupported, fetch a small bounded result set and sort locally only for presentation.

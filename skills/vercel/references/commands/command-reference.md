# Command Reference

Use this index for generic `vercel` command mechanics before choosing a task-specific operation.

- [Global flags](global-flags.md) covers shared flags such as `--scope`, `--token`, `--cwd`, `--debug`, and `--yes`.
- [Output flags](output-flags.md) covers structured command output.
- [Project linking](project-linking.md) covers `vercel link` and `vercel unlink`.
- [Teams](teams.md) covers team listing and scope switching.
- [Targets](targets.md) covers deployment target lookup.

For task-specific commands, prefer the operation reference linked from `SKILL.md` over this index.

# Project Linking

**Link current directory to a Vercel project** (`vercel.write`):

```bash
vercel link $VERCEL_SCOPE
```

**Unlink current directory** (`vercel.write`):

```bash
vercel unlink $VERCEL_SCOPE --yes
```

This is destructive. Only run when the user explicitly asks.

# Git Integration

**Connect a Git repository** (`vercel.write`):

```bash
vercel git connect <git-url> $VERCEL_SCOPE
```

**Disconnect Git** (`vercel.write`):

```bash
vercel git disconnect $VERCEL_SCOPE --yes
```

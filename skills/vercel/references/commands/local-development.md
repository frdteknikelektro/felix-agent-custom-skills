# Local Development

**Run local dev server** (`vercel.write`):

```bash
vercel dev $VERCEL_SCOPE
vercel dev --listen <port> $VERCEL_SCOPE
```

**Initialize a new Vercel project** (`vercel.write`):

```bash
vercel init [example-name] $VERCEL_SCOPE
```
